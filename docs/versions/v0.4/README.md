# FLUED v0.4 / v1

最小假设阶段：验证可微 byte boundary、soft assignment 和 tied inverse decoder 能否形成可逆压缩。

- [`EXPERIMENT_PLAN.md`](EXPERIMENT_PLAN.md)：当时的实验计划和公平性约束。
- 相关实现保留在 `flued/model.py`、`flued/e1_stage_a.py` 等稳定兼容路径。

历史 E3 的正结果不是当前公平协议下的最终结论。
